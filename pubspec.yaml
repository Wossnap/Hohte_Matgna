name: mobile
description: "A new Flutter project for the HohteMobile application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  
  # Audio & Recording packages
  audioplayers: ^6.1.1
  record: ^6.1.2
  permission_handler: ^11.0.0
  path_provider: ^2.1.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # Remove assets for now
  # assets:
  #   - assets/images/
  #   - assets/audio/